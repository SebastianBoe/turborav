Import('CHISEL_JAR COMMONS_IO_JAR')
Java(
    target = '#build/main/classes',
    source = '.',
    JAVAC = "scalac",
    JAVASUFFIX = ".scala",
    JAVACLASSPATH  = [
         CHISEL_JAR,
         COMMONS_IO_JAR
     ],
     JAVACFLAGS = "-feature -language:reflectiveCalls -Xlint -Xfatal-warnings"
)
turborav_jar = Jar(
    target = '#build/main/turborav.jar',
    source = '#build/main/classes'
)

Return('turborav_jar')
